================================================================================
QUERY-TUNING phrase
================================================================================

QUERY-TUNING with LOOKAHEAD
--------------------------------------------------------------------------------

for each customer query-tuning(lookahead).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)))
  (expression_statement
    (parenthesized_expression
      (identifier))))

================================================================================

QUERY-TUNING with multiple options
--------------------------------------------------------------------------------

for each customer query-tuning(bind-where index-hint no-debug).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (identifier)
      (identifier))))

================================================================================

QUERY-TUNING with CACHE-SIZE
--------------------------------------------------------------------------------

for each customer query-tuning(cache-size 1000 row).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (number_literal))))

================================================================================

QUERY-TUNING with CACHE-SIZE BYTE
--------------------------------------------------------------------------------

for each customer query-tuning(cache-size 32768 byte).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (number_literal))))

================================================================================

QUERY-TUNING with DEBUG SQL
--------------------------------------------------------------------------------

for each customer query-tuning(debug sql).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier))))

================================================================================

QUERY-TUNING with DEBUG EXTENDED
--------------------------------------------------------------------------------

for each customer query-tuning(debug extended).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier))))

================================================================================

QUERY-TUNING with DEBUG EXTENDED and diagnostic option
--------------------------------------------------------------------------------

for each customer query-tuning(debug extended performance).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (identifier))))

================================================================================

QUERY-TUNING with LOOKAHEAD and CACHE-SIZE
--------------------------------------------------------------------------------

for each customer query-tuning(lookahead cache-size 8192).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (number_literal))))

================================================================================

QUERY-TUNING with all boolean options
--------------------------------------------------------------------------------

for each customer query-tuning(
  array-message
  bind-where
  index-hint
  join-by-sqldb
  lookahead
  separate-connection
).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (identifier)
      (identifier)
      (identifier)
      (identifier)
      (identifier))))

================================================================================

QUERY-TUNING with all NO- options
--------------------------------------------------------------------------------

for each customer query-tuning(
  no-array-message
  no-bind-where
  no-index-hint
  no-join-by-sqldb
  no-lookahead
  no-separate-connection
).

--------------------------------------------------------------------------------

(source_code
  (ERROR
    (record
      table: (identifier))
    (ERROR
      (identifier)
      (identifier)
      (identifier)
      (identifier)
      (identifier)
      (identifier))))

================================================================================
