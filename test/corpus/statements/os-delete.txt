================================================================================
OS-DELETE
================================================================================

OS-DELETE VALUE(filePath).
OS-DELETE tmpFile tmpDir RECURSIVE.

--------------------------------------------------------------------------------

(source_code
  (os_delete_statement
    (identifier))
  (os_delete_statement
    (identifier)
    (identifier)))

================================================================================
OS-DELETE - reference snippets and RECURSIVE
================================================================================

DEFINE VARIABLE filename AS CHARACTER NO-UNDO.
DEFINE VARIABLE OKpressed AS LOGICAL NO-UNDO INITIAL TRUE.
Main:
REPEAT:
SYSTEM-DIALOG GET-FILE filename
TITLE "Choose File to Delete"
MUST-EXIST
USE-FILENAME
UPDATE OKpressed.
IF OKpressed = FALSE THEN LEAVE Main.
ELSE OS-DELETE VALUE(filename).
END.

OS-DELETE VALUE("tmp") RECURSIVE.

--------------------------------------------------------------------------------

(source_code
  (variable_definition
    (identifier)
    (identifier)
    (no_undo))
  (variable_definition
    (identifier)
    (identifier)
    (no_undo)
    (boolean_literal))
  (repeat_statement
    (identifier)
    (body
      (system_dialog_get_file_statement
        (identifier)
        (string_literal)
        (identifier))
      (if_statement
        (binary_expression
          (identifier)
          (boolean_literal))
        (leave_statement
          (identifier))
        (os_delete_statement
          (identifier)))))
  (os_delete_statement
    (string_literal)))

================================================================================
OS-DELETE - multiple targets with RECURSIVE
================================================================================

OS-DELETE VALUE(cPath) "tmp" RECURSIVE.

--------------------------------------------------------------------------------

(source_code
  (os_delete_statement
    (identifier)
    (string_literal)))
